import React from 'react'

const OverviewTabOverview = ({ data }) => {
    return (
        <div>
            { data ? data : '!!! data null !!!' }
        </div>
    )
}

export default OverviewTabOverview